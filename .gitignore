# regenerable raw data (run run_data.py / run_data_15m.py)
data/*.csv
# bulk per-trade logs (regenerable via run_all.py; summaries are tracked)
results2/trades_*.csv
results/trades_*.csv
# logs & temp
*.log
__pycache__/
*.pyc
quant_lab_release.zip
smoke_15m.py
# COT archives are re-downloadable via run_cot.py / the refresh workflow
data/external/cot/*.zip

# live extension IS tracked (small, needed by the dashboard)
!data/live/

# compact daily series + live extension ARE tracked (CI needs them)
!data/daily/
